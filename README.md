# battery-monitor
A simple battery monitor script that can be run as a systemd service to allow laptops that use conservation mode (charging threshold set to 60%) to extend the threshold to a custom threshold. The threshold in this script is set to 80%.
